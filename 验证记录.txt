小怪破甲 · Windows CMD 自动版 1.4.0 验证记录

一、本版实现

新增 ACE 待资料模板与同一任务资料续接提示词。
新增减少内部指令误回显的提示词，以及只读、脱敏的提示词暴露检查。
新增“准备ACE资料.cmd”“检查提示词暴露.cmd”，EXE 已包含两个新增 Python 模块。
模板不保存材料、不在后台自动继续；不是已实现的 ACE 绕过功能。
暴露检查不输出提示词正文、密钥、完整 URL 或代理凭据，不改变代理或证书。
本机、实际 API 接收方或能解密请求的代理仍可能读取提示词；本版不是防抓包工具。

二、实际回归

验证日期：2026-09-15。
实际平台：Windows 10 x64，10.0.19045。
Python：3.12.14，AMD64。
完整回归：276 项，274 项通过，2 项跳过，0 失败，0 错误。
本次完整回归耗时：72.859 秒。

按模块计数：
- 配置引擎 23 项。
- Windows 入口、身份与旧版升级 45 项。
- 引用修复与并发保护 21 项。
- Mac Python 入口 26 项。
- Mac Shell 35 项。
- Mac 运行时 bootstrap / 原子发布 44 项。
- Mac 打包器 32 项（其中 2 项跳过）。
- 提示词暴露检查 18 项。
- 新提示词与双平台功能入口 19 项。
- Windows 白名单打包器 13 项。

跳过项目：
- test_default_build_refuses_dangling_output_symlink
- test_direct_source_symlink_is_rejected
原因：当前 Windows 创建原生符号链接返回 WinError 1314。
真实 Windows 目录联接、发布竞争和 Bash 可识别的链接用例已执行。

全部配置测试使用独立临时 HOME / USERPROFILE / CODEX_HOME。
不读取或修改用户真实指令与认证文件；不启动真实 Codex，不调用模型。
Mac 的平台、open 调用及 Darwin 原子发布使用模拟或 fixture，不等于 Mac 实机执行。


三、EXE 与入口

构建环境：Windows 10 x64，Python 3.12.14，PyInstaller 6.22.2。
原任务已使用 build-requirements.txt 中的依赖完成 v1.4.0 EXE 构建。
本次续做实际执行该 EXE 和 CMD 的隔离回归，验证安装、状态、恢复、两个新只读动作，
以及中文、空格、& 和 ! 路径、未安装 Python 的精简 PATH。
安装保留原文件和首次备份；重复安装零写入；旧引用修复与正确恢复继续通过。
问候、诗句、频道和 QQ 群口令不变；第二行中间为 4 个 ASCII 空格。

安装器 SHA256：
A9A3BD5713C70B503CF4AFB917D476B678F1B04ECAD3CD9EF6568B1DAA73D290

四、发布与保留

使用 build_windows_package.py 的显式公开文件白名单打包。
打包器检查成员名单、UTF-8/CMD 换行、Python 语法、EXE 文件头、CRC 和逐文件字节。
发布后另行回读安装包并运行解压后的测试；最终 ZIP 的 SHA256 见包外 SHA256SUMS.txt。
5 个历史 ZIP、2 张原项目图片、config_engine.py 和 Mac 运行时下载/发布组件与升级前基线一致。
源码包和安装包不包含 .codex、auth.json、实际配置、指令备份、浏览器数据或测试日志。

尚未执行：模型端问候或保密效果评测、ACE 目标实现验证、Mac 实机验收。
本轮为本地完成与交付，不表示 GitHub 已发布新版。
